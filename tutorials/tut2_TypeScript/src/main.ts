// Variables
const hello = 'wolrd';
let strHello = 'world';

//Specify type
let intNumber: number = 3;

// Functions
const getFullName = (name: string, surname: string): string => {
    return name + ' ' + surname;
}

getFullName("Katlego", "Maredi");